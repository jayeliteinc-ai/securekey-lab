<Window x:Class="iPhoneTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="iPhone Tool" Height="360" Width="640">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <Button Name="RefreshDevicesButton" Click="RefreshDevicesButton_Click" Padding="12,6" Margin="0,0,8,0">Refresh Devices</Button>
            <Button Name="RestoreButton" Click="RestoreButton_Click" Padding="12,6">Restore Selected</Button>
        </StackPanel>

        <ComboBox Name="DeviceSelector" Grid.Row="1" Height="28" Margin="0,0,0,12" DisplayMemberPath="DisplayName" SelectedValuePath="UdId" />

        <TextBox Name="LogOutput"
                 Grid.Row="2"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 AcceptsReturn="True" />
    </Grid>
</Window>
